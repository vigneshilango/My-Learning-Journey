{
  "name": "six-pack-challenge",
  "version": "1.0.0",
  "private": true,
  "description": "6-Pack Challenge — local fitness tracker (Android)",
  "scripts": {
    "split": "node tools/split-app.mjs",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "android:build": "npx cap sync android && cd android && .\\gradlew assembleDebug"
  },
  "capacitor": {
    "platforms": ["android"]
  },
  "dependencies": {
    "@capacitor/android": "^7.2.0",
    "@capacitor/core": "^7.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.2.0"
  }
}
